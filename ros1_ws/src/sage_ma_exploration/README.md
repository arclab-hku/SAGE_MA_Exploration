# ROS Package

This directory contains the public ROS interface and clean deployment templates for multi-agent exploration.

The public scaffold includes launch/configuration hooks for the operator pipeline used by the online policy path. The full release will add the implementation modules for mapping, graph construction, learned utility estimation, planning, communication, and visualization. Trained weights and datasets are intentionally excluded from this public shell.
