# ROS Package

This package contains SAGE ROS interfaces, launch files, and configuration templates.

The algorithmic and training components can be inspected independently from the ROS runtime under:

- `training/gnn_marl_planner/`
- `training/sim/`
